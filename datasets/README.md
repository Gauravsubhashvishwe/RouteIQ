# Datasets

Generated datasets should live here during development, then be copied or symlinked to `backend/data/indian_places.json`.

The committed backend seed data is intentionally small. The application schema supports thousands of OSM-derived locations.
